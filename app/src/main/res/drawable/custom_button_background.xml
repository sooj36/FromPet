<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 내가 만든 그라데이션 배경 설정 -->
    <item>
        <shape>
            <gradient
                android:angle="0"
                android:startColor="@color/accent_color"

                android:endColor="@color/main_color"
                android:type="linear" />
            <corners android:radius="100dp" /> <!-- 원하는 radius 값을 설정 -->
        </shape>
    </item>
</layer-list>
